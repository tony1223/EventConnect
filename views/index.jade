!!! 5
html
	include header
	body
		a(href='/admin/new',style="float:right") Create new Event
		table.table.table-striped
			thead
				tr
					th #
					th EventName
					th EventStartDate
					th EventEndDate
			tbody
			each event, i in events
				tr
					td #{i+1}
					td: a(href='/event/' + event._id) #{event.name}	
					td #{dateFormat(event.startDate)}
					td #{dateFormat(event.endDate)}

		